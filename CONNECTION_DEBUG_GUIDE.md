# Flutter App ????????

## ????

??? CLI ?????? ticket ??Flutter app ?????? "connection failed"?

## ????

????????????????

### 1. **???????**

? `app/rust/src/message_bridge.rs` ??

```rust
// ? 115-126 ?
info!("Using mock connection for address: {}", endpoint_addr_str);

// ??????
let connection_id = format!("mock_conn_{}", uuid::Uuid::new_v4());

// TODO: ????? QUIC ??
// 1. ?? CLI ???????
// 2. ????? EndpointAddr
// 3. ????? QUIC ??
info!("Mock connection established: {}", connection_id);
```

**???** ?????? mock?????????????? QUIC ???

### 2. **Ticket ????????**

? `connect_by_ticket` ????? 613-630 ???

```rust
pub async fn connect_by_ticket(
    mut client: FlutterMessageClient,
    ticket: String,
) -> Result<String> {
    info!("Flutter: Connecting to CLI server using ticket");

    // ????
    let node_id_str = parse_connection_ticket(ticket)?;

    // ?????????
    client.ensure_initialized(None).await?;

    // ????
    let session_id = connect_by_node_id(client, node_id_str, None).await?;
    
    // ...
}
```

`connect_by_node_id` ???? 634-689 ???????????? ID????????? CLI ????

### 3. **???????**

? Flutter ??`connect_screen_simple.dart` ? 536-540 ???

```dart
} catch (e) {
    if (context.mounted) {
      appStore.setError('Failed to connect via ticket: $e');
      appStore.setStatusMessage('Connection failed');
    }
}
```

??????? "Connection failed"?????????????

## ????

### ?? 1: ??????

#### 1.1 ?? Rust ????

```bash
# ??? Flutter app ????????
export RUST_LOG=debug,iroh=debug,quic=debug,riterm=debug

# ?? Flutter app
cd app
flutter run
```

#### 1.2 ?? Flutter ??

```bash
# ??????????
flutter logs | grep -E "Flutter:|Rust:|Error|connection|ticket"
```

### ?? 2: ?? CLI ?????

#### 2.1 ?? CLI ???????

```bash
cd cli
cargo run -- host
```

???????????

```
?? RiTerm Host Server Started
?? Node ID: <node_id>

?? Connection Ticket:
???????????????????????????????????????????????????????????????
? ticket:ABCDEFG...
???????????????????????????????????????????????????????????????
```

#### 2.2 ?? Ticket ??

Ticket ???
- ? `ticket:` ??
- ??? Base32 ?????
- ????? 100-200 ????

### ?? 3: ?????????

?? Flutter ??????????????

## ??????

### ?? 1: ???????????????

?? `app/lib/screens/connect_screen_simple.dart`?

```dart
void _connectWithTicket(BuildContext context) async {
  final ticket = appStore.ticketInput.trim();

  if (ticket.isEmpty) {
    appStore.setStatusMessage('Please enter a connection ticket');
    return;
  }

  // ????? ticket ??????
  debugPrint('=== Connection Debug ===');
  debugPrint('Ticket: ${ticket.substring(0, min(50, ticket.length))}...');
  debugPrint('Ticket length: ${ticket.length}');

  if (!_validateTicket(ticket)) {
    appStore.setError(
      'Invalid ticket format. Ticket should start with "ticket:"',
    );
    appStore.setStatusMessage('Invalid ticket format');
    return;
  }

  appStore.setLoading(true);
  appStore.clearError();
  appStore.setStatusMessage('Connecting using ticket...');

  try {
    debugPrint('Step 1: Creating message client...');
    final client = await createMessageClient();
    debugPrint('Step 2: Client created successfully');
    
    debugPrint('Step 3: Connecting by ticket...');
    final sessionId = await connectByTicket(client: client, ticket: ticket);
    debugPrint('Step 4: Connected! Session ID: $sessionId');

    // ... ????
  } catch (e, stackTrace) {
    // ???????????
    debugPrint('=== Connection Error ===');
    debugPrint('Error: $e');
    debugPrint('Stack trace: $stackTrace');
    
    if (context.mounted) {
      // ??????????
      final errorMessage = e.toString();
      appStore.setError('Failed to connect: $errorMessage');
      appStore.setStatusMessage('Connection failed: ${errorMessage.length > 50 ? errorMessage.substring(0, 50) + "..." : errorMessage}');
    }
  } finally {
    if (context.mounted) {
      appStore.setLoading(false);
    }
  }
}
```

### ?? 2: ?? Rust ??????

? `app/rust/src/message_bridge.rs` ????????

```rust
pub async fn connect_by_ticket(
    mut client: FlutterMessageClient,
    ticket: String,
) -> Result<String> {
    info!("=== Connect by Ticket Debug ===");
    info!("Ticket length: {}", ticket.len());
    info!("Ticket prefix: {}", &ticket[..min(20, ticket.len())]);

    // ????
    match parse_connection_ticket(ticket.clone()) {
        Ok(node_id_str) => {
            info!("Ticket parsed successfully, Node ID: {}", node_id_str);
            
            // ?????????
            match client.ensure_initialized(None).await {
                Ok(_) => info!("Client initialized successfully"),
                Err(e) => {
                    error!("Failed to initialize client: {}", e);
                    return Err(e);
                }
            }

            // ????
            match connect_by_node_id(client, node_id_str.clone(), None).await {
                Ok(session_id) => {
                    info!("Connected successfully, session: {}", session_id);
                    Ok(session_id)
                }
                Err(e) => {
                    error!("Failed to connect by node ID: {}", e);
                    Err(e)
                }
            }
        }
        Err(e) => {
            error!("Failed to parse ticket: {}", e);
            Err(e)
        }
    }
}
```

### ?? 3: ?? Ticket ??

??????????? ticket ???

```rust
// ? app/rust/src/message_bridge.rs ???????
#[frb]
pub fn test_ticket_parsing(ticket: String) -> Result<String> {
    info!("Testing ticket parsing...");
    
    if !ticket.starts_with("ticket:") {
        return Err(anyhow::anyhow!("Ticket must start with 'ticket:'"));
    }
    
    let encoded_data = &ticket[7..];
    info!("Encoded data length: {}", encoded_data.len());
    
    use data_encoding::BASE32;
    match BASE32.decode(encoded_data.as_bytes()) {
        Ok(decoded_data) => {
            info!("Decoded data length: {}", decoded_data.len());
            
            match serde_json::from_slice::<serde_json::Value>(&decoded_data) {
                Ok(ticket_data) => {
                    let result = serde_json::to_string_pretty(&ticket_data)?;
                    info!("Ticket data: {}", result);
                    Ok(result)
                }
                Err(e) => Err(anyhow::anyhow!("Failed to parse JSON: {}", e))
            }
        }
        Err(e) => Err(anyhow::anyhow!("Failed to decode Base32: {}", e))
    }
}
```

## ?????????

### ?? 1: "Invalid ticket format"

**???** Ticket ?????????

**?????**
1. ??????? ticket??? `ticket:` ??
2. ??????????????
3. ?? QR ???????????

### ?? 2: "Failed to decode ticket"

**???** Base32 ????

**?????**
1. ?? CLI ? App ?????????
2. ?? ticket ????????
3. ?? CLI ??? App ????

### ?? 3: "Missing node_id in ticket"

**???** Ticket JSON ?????

**?????**
1. ?? CLI ? `generate_connection_ticket` ??
2. ????? ticket ?????????
   - `node_id`
   - `relay_url` (??)
   - `alpn`

### ?? 4: "Mock connection" ? "TODO: ????? QUIC ??"

**???** ?????? - ?????????

**?????**
???????? QUIC ????????"??????"?

## ????????????

???????????????????

### 1. ????? QUIC ??

? `app/rust/src/message_bridge.rs` ??

```rust
pub async fn connect_by_node_id(
    mut client: FlutterMessageClient,
    node_id_str: String,
    relay_url: Option<String>,
) -> Result<String> {
    info!("Flutter: Connecting to CLI server by node ID: {}", node_id_str);

    // ?????????
    client.ensure_initialized(relay_url.clone()).await?;

    // ????ID
    use iroh::PublicKey;
    use std::str::FromStr;

    let node_id = PublicKey::from_str(&node_id_str)
        .map_err(|e| anyhow::anyhow!("Invalid node ID: {}", e))?;

    // ===== ?????? =====
    if let Some(quic_client) = &mut client.quic_client {
        // ?? NodeAddr
        let mut node_addr = iroh::NodeAddr::new(node_id);
        
        // ??? relay URL???? node_addr
        if let Some(relay) = relay_url {
            // ????? relay URL
            // node_addr = node_addr.with_relay_url(relay)?;
        }

        // ????? QUIC ??
        let connection = quic_client.endpoint
            .connect(node_addr, b"riterm_quic")
            .await
            .map_err(|e| anyhow::anyhow!("Failed to connect: {}", e))?;

        let connection_id = format!("conn_{}", uuid::Uuid::new_v4());
        
        // ????
        {
            let mut connections = quic_client.server_connections.write().await;
            connections.insert(connection_id.clone(), connection);
        }

        // ????
        let session_id = format!("session_{}", uuid::Uuid::new_v4());
        // ... ????
    }
    
    // ...
}
```

### 2. ????????

```rust
use tokio::time::{timeout, Duration};

// ????????
let connection = timeout(
    Duration::from_secs(30),
    quic_client.endpoint.connect(node_addr, b"riterm_quic")
).await
    .map_err(|_| anyhow::anyhow!("Connection timeout after 30 seconds"))?
    .map_err(|e| anyhow::anyhow!("Failed to connect: {}", e))?;
```

### 3. ????????

```rust
// ??????
pub async fn check_connection_status(
    client: &FlutterMessageClient,
    session_id: &str,
) -> Result<bool> {
    // ??????????
    let connections = client.server_connections.read().await;
    if let Some(conn_id) = connections.get(session_id) {
        // ??????
        // ??????
        Ok(true)
    } else {
        Ok(false)
    }
}
```

## ????

### 1. ??????

```bash
# ?? 1: ?? CLI ???
cd cli
RUST_LOG=debug cargo run -- host

# ?? 2: ?? Flutter app??????
cd app
export RUST_LOG=debug
flutter run

# ?? 3: ????
flutter logs | grep -E "connection|ticket|Error"
```

### 2. Ticket ????

? Flutter app ????????

```dart
// ? connect_screen_simple.dart ???
ElevatedButton(
  onPressed: () async {
    try {
      final result = await testTicketParsing(ticket: appStore.ticketInput);
      debugPrint('Ticket parsing result: $result');
      appStore.setStatusMessage('Ticket valid: $result');
    } catch (e) {
      debugPrint('Ticket parsing failed: $e');
      appStore.setError('Ticket invalid: $e');
    }
  },
  child: Text('Test Ticket'),
)
```

### 3. ??????

```bash
# ??????
ping <cli_server_ip>

# ????????????????
nc -zv <cli_server_ip> <port>
```

## ????

### ???????????

```
[DEBUG] Flutter: Connecting to CLI server using ticket
[DEBUG] Parsing iroh connection ticket: ticket:...
[DEBUG] Ticket parsed successfully - Node ID: ..., Relay: ...
[DEBUG] Initializing QUIC message client...
[DEBUG] Client initialized successfully
[DEBUG] Connecting to node: ...
[DEBUG] QUIC connection established
[DEBUG] Flutter: Connected to CLI server using ticket, session: session_...
```

### ???????

```
[ERROR] Failed to parse ticket: Invalid ticket format
[ERROR] Failed to decode ticket: Invalid Base32 character
[ERROR] Failed to connect: Connection refused
[ERROR] Failed to connect: Connection timeout
```

## ?????

1. **?????** ???????????? 1 ??? 2?
2. **???** ???????????
3. **???** ????????????
4. **???** ???????????????

## ?????

??????????????

1. CLI ??????????? ticket?
2. Flutter app ???????? `flutter logs`?
3. Rust ????? `RUST_LOG=debug` ?????
4. ???????????????????????

## ????

- [Iroh ??](https://iroh.computer/docs)
- [QUIC ??](https://www.rfc-editor.org/rfc/rfc9000.html)
- [Flutter Rust Bridge ??](https://cjycode.com/flutter_rust_bridge/)
