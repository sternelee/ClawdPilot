# Flutter ????

???????? RiTerm Flutter ????????

## 1. ??????

### 1.1 ?? debugPrint

???????? `debugPrint` ???????

```dart
debugPrint('????: $variable');
debugPrint('????: $e');
```

**???**
- ?? Debug ??????Release ???????
- ???? Flutter ???????? `print()` ???
- ????????

**?????**
```bash
# ??????
flutter logs

# ??????
flutter logs | grep "????"
```

### 1.2 ?? print()

```dart
print('????: $value');
```

**???** `print()` ? Release ???????????? Flutter ??????????? `debugPrint`?

### 1.3 ????

```dart
assert(condition, '????');
// ?? Debug ?????
```

## 2. IDE ???

### 2.1 VS Code ??

1. **?????**
   - ????????????
   - ???????????

2. **?????**
   - ? `F5` ???"?????"
   - ?? "Flutter" ??
   - ????????`Cmd+Shift+P` ? "Flutter: Launch Emulator"

3. **?????**
   - `F5` - ????
   - `F10` - ????
   - `F11` - ????
   - `Shift+F11` - ????
   - `Shift+F5` - ????

4. **?????**
   - ???????????
   - ?"?????"??????
   - ?"??"????????

### 2.2 Android Studio ??

1. **?????** ??????
2. **?????** ??"Debug"??????????
3. **?????** ???????????
   - `F8` - ??
   - `F7` - ????
   - `Shift+F8` - ????
   - `F9` - ????

## 3. ????

### 3.1 ?? Flutter ??

```bash
# ??????
flutter logs

# ?????????
flutter logs -d <device-id>

# ?????????
flutter devices
```

### 3.2 ????

```bash
# ?? grep ??
flutter logs | grep "Error\|Warning"

# ???????
flutter logs > debug.log
```

### 3.3 ???????

?????????????

```dart
import 'package:flutter/foundation.dart';

void debugLog(String tag, String message, [Object? error]) {
  if (kDebugMode) {
    final timestamp = DateTime.now().toIso8601String();
    debugPrint('[$timestamp] [$tag] $message');
    if (error != null) {
      debugPrint('[$timestamp] [$tag] Error: $error');
    }
  }
}

// ????
debugLog('AppStore', '???????');
debugLog('Terminal', '??????', e);
```

## 4. Rust Bridge ??

??????? flutter_rust_bridge??????????

### 4.1 Rust ???

Rust ???? `tracing` ? `log` ????????

```bash
# ?? Rust ????
export RUST_LOG=debug

# ?? Flutter ??
cd app
flutter run
```

### 4.2 ?? Rust ??

Rust ?????? Flutter ???????????????

```bash
# ?? Rust ????
flutter logs | grep -i "rust\|bridge"

# ????????? Rust?
flutter logs
```

### 4.3 Bridge ????

???? bridge ????????

```dart
try {
  await RustLib.someMethod();
} catch (e, stackTrace) {
  debugPrint('Bridge ????: $e');
  debugPrint('????: $stackTrace');
}
```

## 5. ????

### 5.1 Flutter DevTools

1. **?? DevTools?**
   ```bash
   flutter pub global activate devtools
   flutter pub global run devtools
   ```

2. **?? DevTools?**
   - ??????? DevTools URL
   - ?? VS Code ??? "Flutter: Open DevTools"

3. **???**
   - **Performance** - ????
   - **Memory** - ????
   - **Network** - ????
   - **Widget Inspector** - Widget ???

### 5.2 ????

```bash
# ??????
flutter run --profile

# ??????
flutter run --profile --trace-startup
```

### 5.3 ??????

```dart
// ? main.dart ???
import 'package:flutter/foundation.dart';

void main() {
  // ????????
  if (kDebugMode) {
    debugPrint('????????');
  }
  // ... ????
}
```

## 6. ????

### 6.1 ?? P2P ????

??? P2P ???? iroh????????????

```bash
# ??????
export RUST_LOG=iroh=debug,quic=debug

# ????
flutter run
```

### 6.2 ??????

???????????

```dart
debugPrint('P2P ????: $connectionState');
debugPrint('????: $messageType');
debugPrint('????: $message');
```

## 7. Widget ??

### 7.1 Widget Inspector

? VS Code ? Android Studio ??
- ???????? "Widget Inspector" ??
- ???? Widget ???
- ?? Widget ???

### 7.2 ???????

```bash
# ?????????
? 'r' ?

# ?????????
? 'R' ?

# ????
? 'q' ?
```

## 8. ??????

### 8.1 ??????

???? `flutter_solidart`??????

```dart
// ? AppStore ???????
void debugState() {
  if (kDebugMode) {
    debugPrint('????: ${session.value}');
    debugPrint('????: ${terminals.value}');
    debugPrint('????: ${connectionState.value}');
  }
}
```

### 8.2 ??????

```dart
// ? terminal_view.dart ?
debugPrint('????: $input');
debugPrint('????: $output');
debugPrint('????: $state');
```

### 8.3 ?? P2P ??

```dart
debugPrint('????: $sessionId');
debugPrint('?? ID: $nodeId');
debugPrint('????: $endpointAddr');
debugPrint('?? ID: $connectionId');
```

## 9. ????

### 9.1 ????

? VS Code ??
- ???? ? "????"
- ????????`variable == 'value'`

### 9.2 ????

???????????????

```dart
if (kDebugMode) {
  debugPrint('???: ??? = $variable');
}
```

### 9.3 ??????

```dart
import 'package:flutter/foundation.dart';

if (kDebugMode) {
  // ????????????
  debugPrint('????');
}
```

## 10. ??????

### 10.1 ??????

? `main.dart` ????

```dart
void main() {
  // ?? Flutter ????
  FlutterError.onError = (FlutterErrorDetails details) {
    FlutterError.presentError(details);
    debugPrint('Flutter ??: ${details.exception}');
    debugPrint('??: ${details.stack}');
  };

  // ??????
  PlatformDispatcher.instance.onError = (error, stack) {
    debugPrint('????: $error');
    debugPrint('??: $stack');
    return true;
  };

  runApp(const RiTermApp());
}
```

### 10.2 ????

```dart
class ErrorBoundary extends StatelessWidget {
  final Widget child;
  
  const ErrorBoundary({required this.child});
  
  @override
  Widget build(BuildContext context) {
    return ErrorWidget.builder = (FlutterErrorDetails details) {
      debugPrint('Widget ??: ${details.exception}');
      return Scaffold(
        body: Center(
          child: Text('????: ${details.exception}'),
        ),
      );
    };
  }
}
```

## 11. ??????

```bash
# ??????
flutter devices

# ????
flutter run

# ???????
flutter run -d <device-id>

# ??????
flutter run --debug

# ??????
flutter run --profile

# ????
flutter logs

# ????
flutter clean

# ????
flutter pub get

# ????
flutter analyze

# ????
flutter test
```

## 12. ??????

1. **?? debugPrint ??? print**
   - ??? Release ?????
   - ????????

2. **??????????**
   ```dart
   debugPrint('[AppStore] ????: $state');
   debugPrint('[Terminal] ????: $terminalId');
   ```

3. **??????**
   ```dart
   if (kDebugMode) {
     // ????
   }
   ```

4. **????????**
   - ??????????
   - ?????????

5. **??????**
   - ?????????????
   - ????????????

## 13. ??????????

### 13.1 Rust Bridge ??

```bash
# ???? bridge ???????? Rust API?
cd app
flutter_rust_bridge_codegen generate \
  --rust-input rust_lib_app \
  --dart-output lib/bridge_generated.dart
```

### 13.2 P2P ????

```bash
# ????????
export RUST_LOG=debug,iroh=debug,quic=debug

# ????
flutter run
```

### 13.3 ??????

? `terminal_view.dart` ????

```dart
void _debugTerminalState() {
  if (kDebugMode) {
    debugPrint('=== ?????? ===');
    debugPrint('?? ID: ${terminal.id}');
    debugPrint('?? ID: ${terminal.sessionId}');
    debugPrint('Shell: ${terminal.shellPath}');
    debugPrint('??: ${terminal.state}');
  }
}
```

## ??

- **?????** ?? `debugPrint` ? IDE ???
- **?????** ?? Flutter DevTools
- **?????** ?? `RUST_LOG` ????
- **?????** ????????
- **?????** ????????????

????????
- [Flutter ????](https://docs.flutter.dev/testing/debugging)
- [Flutter DevTools](https://docs.flutter.dev/tools/devtools)
- [flutter_rust_bridge ??](https://cjycode.com/flutter_rust_bridge/)
