# Flutter App ????????

## ????

??? CLI ?????? ticket ??Flutter app ?????? "connection failed"?

## ????

?????????**???????? mock??????????????? QUIC ??**?

### ??

? `app/rust/src/message_bridge.rs` ??

1. **`connect_to_cli_server` ??**?? 105-159 ???
```rust
// ??????????????????? EndpointAddr ??
info!("Using mock connection for address: {}", endpoint_addr_str);

// ??????
let connection_id = format!("mock_conn_{}", uuid::Uuid::new_v4());

// TODO: ????? QUIC ??
```

2. **`connect_by_node_id` ??**?? 664-720 ???
```rust
// ??????ID
let connection_id = format!("conn_{}", uuid::Uuid::new_v4());

// ???????????????
// TODO: ?????????
info!("Node-based connection established: {}", connection_id);
```

## ??????

### 1. ?????????

#### Flutter ??`app/lib/screens/connect_screen_simple.dart`?
- ? ????????
- ? ??????????????
- ? ?? ticket ?????????

#### Rust ??`app/rust/src/message_bridge.rs`?
- ? ? `connect_by_ticket` ???????
- ? ? `parse_connection_ticket` ??????????
- ? ?? `test_ticket_parsing` ??????

### 2. ??????

- ? `CONNECTION_DEBUG_GUIDE.md` - ???????
- ? `DEBUG_CONNECTION.md` - ??????
- ? `debug_connection.sh` - ???????
- ? `FLUTTER_DEBUG_GUIDE.md` - Flutter ??????

## ????????

### ?? 1: ???????

```bash
# ????????
./debug_connection.sh

# ????:
# 1) ?? CLI ??????????
# 2) ?? Flutter App???????
# 3) ?? Flutter ????
# 4) ?? Ticket ??
# 5) ???????
```

### ?? 2: ????

#### ?? 1: ?? CLI ???
```bash
cd cli
RUST_LOG=debug cargo run -- host
```

????? ticket?

#### ?? 2: ?? Flutter App
```bash
cd app
export RUST_LOG=debug
flutter run
```

#### ?? 3: ????
```bash
# ??????
flutter logs | grep -E "Connection|Ticket|Error|Step"
```

## ???????

### ?? Ticket ????

??????????

```
[Flutter] === Connection Debug Start ===
[Flutter] Step 1: Creating message client...
[Flutter] Step 2: Message client created successfully
[Flutter] Step 3: Connecting by ticket...

[Rust] === Connect by Ticket Debug Start ===
[Rust] Step 1: Parsing connection ticket...
[Rust] === Parsing Connection Ticket ===
[Rust] Base32 decode successful
[Rust] JSON parse successful
[Rust] Ticket parsed successfully - Node ID: abc123...
[Rust] Step 1: Ticket parsed successfully
[Rust] Step 2: Client initialized successfully
[Rust] Step 3: Connected successfully, session: session_...

[Flutter] Step 4: Connected! Session ID: session_...
```

**??**????? "Connected successfully"????? mock ??????????

### ?? Ticket ????

??????????

```
[Rust] Failed to decode Base32: Invalid character at position 42
```

?

```
[Rust] Missing 'node_id' field in ticket
```

## ?????

### ?????????????

1. **??????**
   ```bash
   ./debug_connection.sh
   ```

2. **?? Ticket ??**
   - ???? 4
   - ?? CLI ??? ticket
   - ??????

3. **??????**
   - ?? CLI ?????? 1?
   - ?? Flutter App??? 2?
   - ????????? 3?
   - ???????????

### ?????????????

???????????????????

#### 1. ????? QUIC ??

? `app/rust/src/message_bridge.rs` ? `connect_by_node_id` ????

```rust
pub async fn connect_by_node_id(
    mut client: FlutterMessageClient,
    node_id_str: String,
    relay_url: Option<String>,
) -> Result<String> {
    // ... ???? ...

    // ????ID
    let node_id = PublicKey::from_str(&node_id_str)?;

    // ??????
    if let Some(quic_client) = &mut client.quic_client {
        // ?? NodeAddr
        let node_addr = iroh::NodeAddr::new(node_id);
        
        // ?? QUIC ??
        let connection = quic_client.endpoint
            .connect(node_addr, b"riterm_quic")
            .await?;
        
        // ??????
        let connection_id = format!("conn_{}", uuid::Uuid::new_v4());
        quic_client.server_connections.write().await
            .insert(connection_id.clone(), connection);
        
        // ... ???? ...
    }
    
    // ...
}
```

#### 2. ????????

?????
- ??????
- ?????/????
- ?????????

#### 3. ????????

???
- ????
- ??????
- ????

## ????

### Ticket ??

CLI ??? ticket ???

```
ticket:<BASE32_ENCODED_JSON>
```

???? JSON ???

```json
{
  "node_id": "abc123...",
  "relay_url": null,
  "alpn": "riterm_quic"
}
```

### ????

???????????

1. **?? Ticket** ? ?? node_id ? relay_url
2. **??? Endpoint** ? ?? iroh QUIC endpoint
3. **?? NodeAddr** ? ?? node_id ? relay_url
4. **????** ? ?? `endpoint.connect()`
5. **????** ? ?????????
6. **????** ? ??/????

### ???????

?????????? 1?2 ? 5?**???? 3 ? 4**??????????

## ????

### Q: ?????? "Connected successfully" ????????

A: ???????? mock ???????????? connection_id??????????????

### Q: Ticket ??????????????

A: Ticket ???????????? ticket ???????????????????????? QUIC ?????

### Q: ???? CLI ??????????

A: 
1. ?? CLI ?????? "RiTerm Host Server Started"
2. ??? Connection Ticket ??
3. ?? `ps aux | grep riterm` ????

### Q: ???????????

A: ??????????????????
- NAT ??
- ?????
- Relay ??????

## ????

- `CONNECTION_DEBUG_GUIDE.md` - ???????
- `DEBUG_CONNECTION.md` - ??????
- `FLUTTER_DEBUG_GUIDE.md` - Flutter ????
- `CLAUDE.md` - ??????

## ??

**?????**
- ? Ticket ?????????
- ? ??????????
- ? ???????
- ? **??? QUIC ?????**??????
- ? ?????????
- ? ?????????

**???????**
1. **?????** ????? QUIC ???`connect_by_node_id`?
2. **?????** ????????
3. **?????** ?????????????

**???????**
1. ???????? ticket ??
2. ?????????
3. ??????? mock ?????

**??????**
1. ????? QUIC ????
2. ???????????
3. ?????????????
