# ????????

## ??

?? `cargo check -p rust_lib_app` ????????

```
error[E0433]: failed to resolve: could not find `NodeAddr` in `iroh`
```

## ??

? iroh 0.94 ????`NodeAddr` ?? `iroh` ????????????????? `PublicKey` ??? `EndpointAddr`?

## ??

### ??????

```rust
// ?????
let node_addr = iroh::NodeAddr::new(node_id);
let endpoint_addr = iroh::EndpointAddr::from(node_addr);
```

### ??????

```rust
// ????? - ??? PublicKey ?? EndpointAddr
let endpoint_addr = iroh::EndpointAddr::from(node_id);
```

## ?????

**???** `app/rust/src/message_bridge.rs`

**???** `connect_by_node_id` (? 865-973 ?)

**?????**

```rust
// ?? EndpointAddr
info!("Building endpoint address...");

// ?? PublicKey ???? EndpointAddr
// iroh 0.94 ??EndpointAddr ??? PublicKey ??
if let Some(relay) = &relay_url {
    info!("Using relay URL: {}", relay);
    info!("Note: Relay URL support may require additional configuration");
} else {
    info!("No relay URL, using direct connection");
}

// ??? PublicKey ?? EndpointAddr
let endpoint_addr = iroh::EndpointAddr::from(node_id);
info!("Endpoint address built: {:?}", endpoint_addr);
```

## ??

???????????

```bash
cd app/rust
cargo check
```

**?????**

```
    Checking rust_lib_app v0.1.0
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 3.91s
```

## ????

### ? ?????

- ????????
- ????????? API
- `EndpointAddr::from(PublicKey)` ? iroh ?????

### ?? Relay URL ??

?????
- ?? relay URL ??
- ?????????
- ?????? iroh ??? API ?? relay ??

## ??

?????????????????

```bash
# 1. ?? CLI ???
cd cli
RUST_LOG=debug cargo run -- host

# 2. ?? Flutter App
cd app
export RUST_LOG=debug
flutter run

# 3. ????
# ? app ??? ticket ???
```

## ????

- `REAL_CONNECTION_IMPLEMENTATION.md` - ????
- `TEST_REAL_CONNECTION.md` - ????
- `????.md` - ????

## ??

? **???????**
- ????? iroh API
- ?????
- ??????

? **??????**
- ????
- ????
- ??????

---

**?????** 2024
**???** ? ???
