# ?? QUIC ??????

## ?? ????

???????? QUIC ??????????? Mock ???

## ?? ????

### 1. ?????`connect_by_node_id`

**???** `app/rust/src/message_bridge.rs` (? 865-973 ?)

**???**
- ???? ID
- ?? EndpointAddr
- ????? QUIC ??
- ?????????
- 30 ???????

**?????**

```rust
// ???Mock ???
let connection_id = format!("mock_conn_{}", uuid::Uuid::new_v4());
// TODO: ????? QUIC ??

// ????????
let connection_result = tokio::time::timeout(
    std::time::Duration::from_secs(30),
    quic_client.connect_to_server(&endpoint_addr)
).await;

let connection_id = match connection_result {
    Ok(Ok(conn_id)) => {
        info!("? QUIC connection established successfully!");
        conn_id
    }
    Ok(Err(e)) => {
        return Err(anyhow::anyhow!("Failed to connect: {}", e));
    }
    Err(_) => {
        return Err(anyhow::anyhow!("Connection timeout"));
    }
};
```

### 2. ?????`connect_to_cli_server`

**???** `app/rust/src/message_bridge.rs` (? 109-145 ?)

**???**
- ?????????
- ??? `connect_by_node_id` ????

### 3. ?????`connect_by_ticket`

**???** `app/rust/src/message_bridge.rs` (? 812-862 ?)

**???**
- ?? ticket
- ?? node_id
- ?? `connect_by_node_id` ????

## ?? ????

### ????

```
1. ?? Ticket
   ?
2. ?? Node ID
   ?
3. ??? QUIC Client
   ?
4. ?? EndpointAddr
   ?
5. ?? QUIC ?? ? (???)
   ?
6. ????
   ?
7. ??????
   ?
8. ?? Session ID
```

### ??? API

- **`iroh::PublicKey::from_str()`** - ???? ID
- **`iroh::NodeAddr::new()`** - ??????
- **`iroh::EndpointAddr::from()`** - ???????
- **`QuicMessageClient::connect_to_server()`** - ?? QUIC ??
- **`tokio::time::timeout()`** - ??????

### ALPN ??

?? `riterm_shared::QUIC_MESSAGE_ALPN` ???????????

```rust
pub const QUIC_MESSAGE_ALPN: &[u8] = b"com.riterm.messages/1";
```

### ????

- **?????** 30 ?
- **?????** ?????????????????

## ?? ????

### ???????

```
[Rust] === Connect by Node ID Start ===
[Rust] Node ID: abc123...
[Rust] Relay URL: None
[Rust] Ensuring client is initialized...
[Rust] Parsing node ID...
[Rust] Node ID parsed successfully: abc123...
[Rust] Building endpoint address...
[Rust] No relay URL, using direct connection
[Rust] Endpoint address built: ...
[Rust] Establishing QUIC connection...
[Rust] Using ALPN: [99, 111, 109, 46, 114, 105, 116, 101, 114, 109, ...]
[Rust] ? QUIC connection established successfully!
[Rust] Connection ID: client_conn_...
[Rust] Creating session...
[Rust] Storing connection and session...
[Rust] Connection stored: session_... -> client_conn_...
[Rust] Session stored: session_...
[Rust] === Connect by Node ID Complete ===
[Rust] ? Successfully connected to CLI server!
[Rust] Session ID: session_...
[Rust] Connection ID: client_conn_...
[Rust] Node ID: abc123...
```

### ???????

#### ????
```
[Rust] Establishing QUIC connection...
[Rust] ? Connection timeout after 30 seconds
[Flutter] Connection failed: Connection timeout: server did not respond within 30 seconds
```

#### ?????
```
[Rust] Establishing QUIC connection...
[Rust] ? Failed to establish QUIC connection: connection refused
[Flutter] Connection failed: Failed to connect to server: connection refused
```

#### ?? ID ??
```
[Rust] Parsing node ID...
[Rust] Failed to parse node ID: invalid hex string
[Flutter] Connection failed: Invalid node ID format: invalid hex string
```

## ?? ????

### 1. ??????

```bash
# ?? 1: ?? CLI ???
cd cli
RUST_LOG=debug cargo run -- host

# ????? ticket

# ?? 2: ?? Flutter App
cd app
export RUST_LOG=debug
flutter run

# ? app ??? ticket ???

# ?? 3: ????
flutter logs | grep -E "Connection|QUIC|?|?"
```

### 2. ??????

?????????

```
? QUIC connection established successfully!
```

????

```
Using mock connection for address: ...
```

### 3. ??????

```bash
# ???????? node ID
# ??? 30 ?????????
```

### 4. ??????

```bash
# ????? ticket ??
# ????? node ID
# ????????
```

## ?? ????

### 1. ??????

```bash
export RUST_LOG=debug,iroh=debug,quic=debug
```

### 2. ??????

? Flutter app ????????????
- ????
- ????
- ????

### 3. ??????

????????
- Connection ID ? `client_conn_` ????? `mock_conn_`?
- ? QUIC ?????????
- ??????????

## ?? ????

### 1. Relay ??

?????? relay URL?????
- CLI ???? ticket ??? relay URL
- ????????? relay URL

### 2. NAT ??

- ?? iroh ? NAT ????
- ???? relay ?????
- ??????????????

### 3. ????

- ??????????
- ?????????????
- ????????????

## ?? ?????

### 1. ??????

```rust
// ??????
pub async fn check_connection_health(
    client: &FlutterMessageClient,
    session_id: &str,
) -> Result<bool> {
    // ??????
    // ????????
}
```

### 2. ????

```rust
// ????????
pub async fn reconnect_if_needed(
    client: &mut FlutterMessageClient,
    session_id: &str,
) -> Result<()> {
    // ??????
    // ??????
}
```

### 3. ?????

```rust
// ??????
pub struct ConnectionPool {
    connections: HashMap<String, Connection>,
    max_connections: usize,
}
```

### 4. ????

- ????
- ????????
- ????????

## ?? ????

?????????

- [ ] ?????? ticket
- [ ] ???? QUIC ??
- [ ] ????????
- [ ] ????????
- [ ] ????????
- [ ] ????????
- [ ] ?????????
- [ ] ???????????
- [ ] ??????????
- [ ] ??????????

## ?? ? Mock ?????

| ?? | Mock ?? | ???? |
|------|----------|---------|
| ???? | ? ? | ? ? |
| ???? | ? ? | ? ? |
| ???? | ? ? | ? 30? |
| ???? | ?? ?? | ? ?? |
| ???? | ?? ?? | ? ?? |
| ??? | ? ??? | ? ???? |

## ?? ????

- `CONNECTION_DEBUG_GUIDE.md` - ????
- `QUICK_DEBUG_REFERENCE.md` - ????
- `CONNECTION_TROUBLESHOOTING_SUMMARY.md` - ????
- `shared/src/quic_server.rs` - QUIC ?????
- `shared/src/event_manager.rs` - ?????

## ?? ????

### ?? 1: "QUIC client not initialized"

**???** ????????

**?????**
```bash
# ?? iroh ???????
cd app/rust
cargo check

# ??????
RUST_LOG=debug flutter run
```

### ?? 2: "Connection timeout"

**???** 
- CLI ??????
- ??????
- ?????

**?????**
```bash
# ?? CLI ?????
ps aux | grep riterm

# ??????
ping <server_ip>

# ???????
```

### ?? 3: "Failed to connect to server"

**???**
- Node ID ??
- ALPN ???
- ???????

**?????**
```bash
# ?? ticket
./debug_connection.sh
# ???? 4 ?? ticket

# ?? CLI ?????
cd cli
RUST_LOG=debug cargo run -- host
```

## ? ????

???????????

```bash
# 1. ???????
cd app
flutter clean
flutter pub get

# 2. ???? bridge ????????
flutter_rust_bridge_codegen generate \
  --rust-input rust_lib_app \
  --dart-output lib/bridge_generated.dart

# 3. ????
flutter test

# 4. ?? app ?????
export RUST_LOG=debug
flutter run
```

## ?? ??

? **????**
- ????? QUIC ????
- ????????
- ?????????
- ??????

? **?????**
- ???????
- ????????
- ?????????

?? **?????**
- ?? CLI ???????
- ????????????
- ?????????? relay ???

?? **????**
- ???????
- ????????
- ??????
- ????????

---

**?????** 2024
**???** 1.0
**???** ? ?????
